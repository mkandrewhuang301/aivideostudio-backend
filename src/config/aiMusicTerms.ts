// Versioned 18+ attestation shared by every surface that invokes Google-powered AI Music.
// Bumping this value makes the server require one fresh acceptance for the new terms version.
export const AI_MUSIC_AGE_TERMS_VERSION = '2026-07-30';
